* have stamps.dat files and python downloaders available
* python downloader:
    - unzip
    - first frame to data folder (see `extract_first_frames.sh`)
* convert .png depth to .ply (Matlab LodToPly)
* sample (using C++ code)
* detect and refine -> .info files
